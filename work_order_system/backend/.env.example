# 后端环境变量模板
#
# 用法：复制本文件为同目录 .env（backend/.env），填入真实值后启动后端即可生效。
# .env 已被 .gitignore 忽略，【禁止】提交真实 .env（含 WX_APPSECRET 机密）。
# 本 .env.example 为安全模板（无真实值），可入库供他人参考。
#
# 全部为可选：未设置时后端以 config.py 默认值运行；
# 注意 WX_PUSH_ENABLED 默认 False，配置完成且确认模板后，再将 config.py 改为 True 开启真实推送。

# 微信小程序 AppID（非敏感，但属账号专属）
# 获取：小程序后台「开发管理 → 开发设置 → 开发者 ID → AppID(小程序ID)」
WX_APPID=your_wx_appid_here

# 微信小程序 AppSecret（【机密】！仅放本 .env 或系统环境变量，绝不入库 / 绝不写前端）
# 获取：小程序后台「开发管理 → 开发设置 → 开发者 ID → AppSecret(小程序密钥)」
# 泄露处理：立即在后台「重置」AppSecret，使旧值失效。
WX_APPSECRET=your_wx_appsecret_here

# 订阅消息模板 ID（非敏感；须与小程序端 TEMPLATE_ID、后端 WX_TEMPLATE_FIELDS 三者一致）
# 获取：小程序后台「功能 → 订阅消息 → 我的模板」，选用/申请含如下关键词的模板：
#       thing1(工单号) / thing2(产品) / number1(数量) / thing3(事项提示)
WX_SUBSCRIBE_TEMPLATE_ID=your_template_id_here
